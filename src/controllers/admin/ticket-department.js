// @ts-check

/**@type {import('express').RequestHandler} */
async function createDepartments(req, res, next) {}

/**@type {import('express').RequestHandler} */
async function readDepartments(req, res, next) {}

/**@type {import('express').RequestHandler} */
async function readDepartment(req, res, next) {}

/**@type {import('express').RequestHandler} */
async function updateDepartment(req, res, next) {}

/**@type {import('express').RequestHandler} */
async function deleteDepartment(req, res, next) {}

module.exports = {
    createDepartments,
    readDepartment,
    readDepartments,
    updateDepartment,
    deleteDepartment,
};
